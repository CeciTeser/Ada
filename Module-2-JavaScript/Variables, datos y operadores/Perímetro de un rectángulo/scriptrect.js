const base = Number (prompt('Ingresá el valor de la base del rectángulo'));
const altura = Number (prompt('Ingresá el valor de la altura del rectángulo'));
const resultado = 2 * (base + altura);

alert ('El resultado del perímetro del rectángulo con base ' + base + ' y altura ' + altura + ' es: ' + resultado);
